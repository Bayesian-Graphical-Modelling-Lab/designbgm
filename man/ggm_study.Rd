% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/family-ggm.R
\name{ggm_study}
\alias{ggm_study}
\title{Constructs an object for simulating Gaussian graphical model prior studies}
\usage{
ggm_study(p, nu, G = NULL, structure = NULL, ...)
}
\arguments{
\item{p}{Number of nodes. Must be at least 3.}

\item{nu}{Prior study size: the number of observations each simulated study
collects. Must exceed \code{p}.}

\item{G}{A \code{p} by \code{p} symmetric 0/1 adjacency matrix with zero
diagonal. Supply this argument to hold the graph fixed across studies, so that only
the estimated precision matrix varies.}

\item{structure}{A graph generator, as in \code{\link{generate_graph}}. Supply
this argument instead of \code{G} to draw a new graph for each study.}

\item{...}{Further arguments for the generator, e.g. \code{prob} for
\code{"structure = Bernoulli"}. Only used with \code{structure}.}
}
\value{
A \code{ggm_study} object, which also inherits from \code{bgm_study}.
}
\description{
Collects the inputs needed to simulate prior studies for a
  Gaussian graphical model, then passes the result to \code{\link{simulate_prior_study}}.
}
\seealso{
Other prior elicitation: 
\code{\link{elicit_prior}()},
\code{\link{elicit_prior.ggm_parameters}()},
\code{\link{simulate_prior_study}()},
\code{\link{simulate_prior_study.ggm_study}()}
}
\concept{prior elicitation}
