% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/family-ggm.R
\name{bsda_control}
\alias{bsda_control}
\title{Control parameters for BSDA sample-size planning}
\usage{
bsda_control(
  gwish_sampler = "direct",
  gwish_tol = 1e-08,
  gwish_iter = 500L,
  gwish_burnin = 500L,
  edge_threshold = 0.5,
  fit_iterations = 10000L,
  fit_burnin = 5000L,
  alpha = 0.05,
  n_scout = 6L,
  n_main = 10L,
  scout_frac = 1/3,
  max_iter = 10L,
  n_boot = 5000L,
  eps = 0.001,
  tol_frac = 0.01,
  verbose = FALSE,
  seed = NULL
)
}
\arguments{
\item{gwish_sampler}{G-Wishart sampler, \code{"direct"} or \code{"block"}.}

\item{gwish_tol, gwish_iter, gwish_burnin}{Tolerance, iterations, and burn-in
for the G-Wishart sampler.}

\item{edge_threshold}{Posterior inclusion probability above which an edge is
selected. Default 0.5.}

\item{fit_iterations, fit_burnin}{MCMC length and burn-in for fitting each
simulated study.}

\item{alpha}{Significance level used in the fit. Default 0.05.}

\item{n_scout, n_main}{Grid sizes for the scout and main search passes.}

\item{scout_frac}{Fraction of \code{fit_iterations} used in the scout pass.}

\item{max_iter}{Maximum refine-evaluate-invert iterations.}

\item{n_boot}{Bootstrap resamples for the probit-inversion CI.}

\item{eps}{Numerical tolerance guarding the probit inversion.}

\item{tol_frac}{Tolerance fraction of the relative sample size difference 
used to stop the search. Default 0.01, matched to typical H = 50. 
Lower it if H raises substantially.}

\item{verbose}{Stream progress from the C++ routine. Default \code{FALSE}.}
}
\value{
A named list of control settings.
}
\description{
Lists the sampler, model-fit, and search-algorithm settings
  for \code{design(method = "BSDA")} and its validation. These are the
  deeper knobs a user rarely changes; the planning targets (\code{measure},
  \code{measure_value}, \code{target_pow}) stay in \code{\link{design}}'s own
  arguments.
}
\seealso{
Other sample size planning: 
\code{\link{design}()},
\code{\link{design.ggm_elicited}()},
\code{\link{validate}()},
\code{\link{validate.ggm_design}()}
}
\concept{sample size planning}
